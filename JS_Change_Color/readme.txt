#Change page background color

In this challange, there is a button on the page. When you push the 
the button, page background color changes randomly.
there are two function. one of them producing random color. Other is changing color function.

#Change: getting color is not randomly.
steps:
create a color array and get colors in this array randomly.
if user enter a new color, background is setted that color and same 
color added the color array.